import { ErButton, ErIcon, ErButtonGroup, ErCollapse, ErCollapseItem, ErAlert, ErTooltip, ErPopconfirm, ErDropdown, ErDropdownItem, ErMessage, ErConfigProvider, ErInput, ErMessageBox, ErNotification } from '@yzy-element/components';
import type { Plugin } from 'vue';

export default [ErButton, ErIcon, ErButtonGroup, ErCollapse, ErCollapseItem, ErAlert, ErTooltip, ErPopconfirm, ErDropdown, ErDropdownItem, ErMessage, ErConfigProvider, ErInput, ErMessageBox, ErNotification] as Plugin[];